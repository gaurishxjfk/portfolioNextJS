import React from "react";
import MainLayout from "../../components/MainLayout";

const page = () => {
  return (
    <MainLayout>
      <section className="mt-[5%] tracking-wide	 w-[80%] sm:w-[45%] md:w-[35%] flex flex-col justify-center mx-auto sm:ml-[16%] md:ml-[22%] transition-all ease-linear duration-700">Updating it soon..</section>
    </MainLayout>
  );
};

export default page;
